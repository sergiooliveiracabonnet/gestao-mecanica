# Variáveis específicas do frontend — o .env.example da raiz cobre o que é
# compartilhado com outros serviços.
# PORT é lido nativamente pelo Next.js (next dev / next start).
PORT=3000
NEXT_PUBLIC_API_URL=http://localhost:3001
