# Infra

Placeholder — Terraform para AWS (ECS Fargate, RDS, Secrets Manager) entra
aqui quando houver uma feature real para deployar em produção (a partir da
Feature 2, IAM). Ver seção 9 de
`docs/superpowers/specs/2026-07-14-arquitetura-mvp-design.md`.

Staging usa Railway (deploy via `railway.toml`, sem Terraform necessário).
