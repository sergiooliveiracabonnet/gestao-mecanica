# API

Documentação de endpoints por módulo. Gerada/atualizada a partir dos DTOs em
`packages/contracts/` conforme cada feature adiciona seus endpoints (ver
convenções RPC-style na seção 7 da spec de arquitetura).

Nesta feature (setup de infra), o único endpoint existente é `GET /health`
— ver `backend/src/health/health.controller.ts`.
